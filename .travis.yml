language: node_js

node_js:
  - '6'

install:
  - npm install

script:
  - npm run lint
  - npm run test
  - npm run build

deploy:
  provider: surge
  project: ./dist/
  domain: glmr-synth.surge.sh
  skip_cleanup: true
